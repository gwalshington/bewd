<%= form_for(@form) do |f| %>
  <% if @form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@form.errors.count, "error") %> prohibited this form from being saved:</h2>

      <ul>
      <% @form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :form_name %><br>
    <%= f.text_field :form_name %>
  </div>
  <div class="field">
    <%= f.label :form_link %><br>
    <%= f.text_field :form_link %>
  </div>
  <div class="field">
    <%= f.collection_select(:municipality_id, Municipality.all, :id, :name) %><br>
  </div>
  <div class="field">
    <%= f.label :department_id %><br>
    <%= f.collection_select :department_id, Department.order(:department), :id, :department %>
  </div>
  <div class="actions">
    <%= f.submit "Add Form"%>
  </div>
<% end %>


